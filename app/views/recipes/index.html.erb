<h1>Searching for - <%= @search_term %></h1>

<% if @recipes.nil? %>
  <p>not found</p>
<% else %>

  <table border = "0">
    <% @recipes.each do |item| %>
      <tr>
        <td><%= link_to image_tag(item["image_url"], height: '100', width: '100'), item["f2f_url"] %></td>
        <td><%= link_to sanitize(item["title"]), item["f2f_url"] %></td>
        <td><%= item["social_rank"] %></td>
      </tr>
    <%end%>
  </table>
<% end %>